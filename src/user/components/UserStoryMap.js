import React from 'react';

import StoryMap from 'storyMap/components/StoryMap';

import config from './config';

const UserStoryMap = () => {
  return <StoryMap config={config} />;
};

export default UserStoryMap;
